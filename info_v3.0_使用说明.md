# 信息采集系统 V3.0 使用说明

---

## 一、系统概述

信息采集系统（采集端）+ 脊柱筛查结果查询系统（查询端）

- **采集端**：`form.php` 终端客户信息录入页面
- **查询端**：`spine_verify.php` → `spine_students.php` → `spine_view.php` 三步查询流程

---

## 二、功能说明

### 2.1 信息采集（form.php）

| 功能 | 说明 |
|------|------|
| 终端录入 | 家长填写学生信息（姓名/性别/出生日期/身份证号/电话等） |
| 预置字段 | 省/市/区/学校/筛查项目由后台预设，客户只读 |
| 验证码 | 数学题防刷（每次提交随机生成） |
| 修改码 | 提交后显示全家共用修改码，用于后续修改和查询 |
| 采集状态 | 进行中可录入/修改；结束后显示查询入口 |

### 2.2 信息修改（form_edit.php）

- 手机号 + 修改码 验证身份
- 验证后可修改本人提交的所有学生信息
- 支持多学生家庭（一个修改码管理全家）

### 2.3 脊柱筛查结果查询（spine_verify.php / spine_students.php / spine_view.php）

- **Step1** 手机号 + 修改码 验证
- **Step2** 选择要查哪个学生
- **Step3** 显示脊柱筛查结果（项目/学校/年级/班级/结论）
- 查询时机：采集结束后自动开放

### 2.4 后台管理（admin/index.php）

| 功能 | 说明 |
|------|------|
| 用户管理 | 新增/编辑普通用户（各自独立管理表单和数据） |
| 表单管理 | 创建/编辑/复制/删除表单，配置预置字段和选项 |
| 脊柱库管理 | 设置脊柱库根目录、扫描发现、手动登记、自动扫描状态 |
| 导出数据 | Excel 导出 |

### 2.5 自动扫描（auto_scan.bat）

- 由 Windows 任务计划程序每1分钟调用一次
- 自动扫描脊柱库根目录，发现新库后登记到系统

---

## 三、目录结构

```
info_v2/
├── form.php                    # 终端录入页面
├── form_edit.php               # 终端修改页面
├── spine_verify.php            # 查询 Step1（身份验证）
├── spine_students.php          # 查询 Step2（学生选择）
├── spine_view.php              # 查询 Step3（结果显示）
├── admin/                      # 后台管理
│   ├── index.php               # 管理主页
│   ├── spine_db.php            # 脊柱库管理
│   ├── __nav.php               # 导航
│   └── ...
├── api/
│   └── submit.php              # 提交接口
├── user/                      # 用户后台
│   ├── index.php               # 用户主页
│   ├── submissions.php          # 数据列表
│   └── ...
├── lib.php                     # 核心函数库
├── db/
│   ├── info_collection.db       # SQLite 数据库
│   └── init.sql                # 数据库初始化脚本
├── assets/
│   ├── style.css
│   ├── mobile_ui.css
│   └── qrcodejs.js
├── auto_scan.php               # 自动扫描脚本
└── auto_scan.bat               # Windows 计划任务调用脚本
```

---

## 四、环境部署（Windows + phpStudy）

### 4.1 基础环境

- **Web服务器**：phpStudy（小皮面板）+ PHP 7.4 + Nginx/Apache
- **数据库**：SQLite3（无需安装，系统自带）
- **脊柱库目录**：`D:\spine_db\`（可自定义）

### 4.2 部署步骤

**Step 1：下载源码**
将 `info_v2` 文件夹放到 phpStudy 网站根目录，如：
```
D:\phpstudy_pro\WWW\info2\
```

**Step 2：配置网站**
在 phpStudy 中添加网站：
- 域名：`info2.test`（本地测试用）
- 网站目录：`D:\phpstudy_pro\WWW\info2\`
- PHP版本：`php7.4.3nts`

**Step 3：设置脊柱库根目录**
脊柱库按「机构名」分目录存放，文件名为 `SpinalMeasurement_Pro.db`：
```
D:\spine_db\测试公司\SpinalMeasurement_Pro.db
D:\spine_db\某学校\SpinalMeasurement_Pro.db
```

**Step 4：访问后台**
- 后台地址：`http://info2.test/admin/`
- 初始账号：`admin` / 密码：`admin123`

**Step 5：创建表单**
1. 用 admin 账号登录后台
2. 新增普通用户（用于管理表单）
3. 用普通用户登录 `http://info2.test/user/`
4. 创建表单，配置预置字段（省/市/区/学校/筛查项目）
5. 保存后获取表单 URL

**Step 6：配置脊柱库**
1. 进入后台 → 「🦴 脊柱库管理」
2. 设置脊柱库根目录（如 `D:\spine_db`）
3. 点击「扫描」发现脊柱库
4. 编辑表单，绑定对应脊柱库路径

### 4.3 自动扫描配置（可选）

**目的**：脊柱库目录变化后自动登记到系统

**操作步骤：**
1. 打开「任务计划程序」（Win+R → `taskschd.msc`）
2. 创建基本任务 → 名称：`脊柱库自动扫描`
3. 触发器：按需求（建议每1分钟）
4. 操作：启动程序 → 程序：`D:\phpstudy_pro\WWW\info2\auto_scan.bat`
5. 完成

---

## 五、脊柱库目录结构规范

```
{脊柱库根目录}/
└── {机构名称}/
    └── SpinalMeasurement_Pro.db
```

**示例：**
```
D:\spine_db\
├── 测试公司\
│   └── SpinalMeasurement_Pro.db
├── 明玮健康\
│   └── SpinalMeasurement_Pro.db
```

- `auto_scan.php` 会自动扫描根目录下各子文件夹
- 表单绑定脊柱库时，只需选择对应机构名称即可

---

## 六、终端客户使用流程

### 采集阶段

1. 家长打开表单链接 → 填写学生信息 → 提交
2. 页面显示「修改码」，妥善保存
3. 提交后可点「点此修改」更正信息

### 查询阶段（采集结束后）

1. 家长打开表单链接 → 页面显示「🔍 信息登记已结束」
2. 点击「🦴 查看脊柱筛查结果」
3. 输入手机号 + 修改码 → 下一步
4. 选择学生 → 查看脊柱筛查结果

---

## 七、字段标签规范（V2.0+）

表单中显示的字段标签与数据库 key 一一对应：

| 数据库 key | 显示标签 | 说明 |
|-----------|---------|------|
| 姓名 | 学生姓名 | |
| 性别 | 学生性别 | |
| 电话 | 监护人电话 | |
| 出生日期 | 学生出生日期 | |
| 身份证号 | 学生身份证号 | |
| 年级 | 年级 | 预置字段 |
| 班级 | 班级 | 预置字段 |
| 省/市/区/学校 | — | 预置，只读 |

---

## 八、常见问题

**Q：修改码丢失怎么办？**
A：联系学校管理员，在后台「数据列表」中查看该手机的提交记录。

**Q：脊柱筛查结果查不到？**
A：确认表单绑定的脊柱库路径与脊柱库管理里登记的路径完全一致。

**Q：自动扫描不生效？**
A：检查 `auto_scan.bat` 路径是否正确，PHP 是否添加到系统 PATH。

**Q：采集关闭后还能修改信息吗？**
A：不可以。采集关闭后 `form_edit.php` 也随之停用，只能查询脊柱结果。
